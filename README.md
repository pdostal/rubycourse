# git commands

* `git commit -am "Your comment`
* `git pull` Download from the server
* `git push` Upload to the server
* `git status` Show the current state
* `git add FILE` # Add a new FILE to git
